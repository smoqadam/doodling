'use client';

import RotatingPlane from '@/sketches/RotatingPlane';

export default function Page() {
  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <RotatingPlane />
    </div>
  );
}
